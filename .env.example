# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/sudoku

# NextAuth.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here

# Socket.io Server URL
# For local development (same server): leave empty or http://localhost:3000
# For production with separate socket server: https://your-socket-server.railway.app
NEXT_PUBLIC_SOCKET_URL=

# Socket Server Environment Variables (for socket-server/)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/sudoku
# FRONTEND_URL=https://your-app.vercel.app
# PORT=3001

git push -f origin main
git checkout main
git reset --hard fd9717e
git checkout main
git pull origin main
git merge 001-improve-shadowing-ui
# Xóa nhánh cục bộ
git branch -d 001-improve-shadowing-ui

# Xóa nhánh trên GitHub (remote)
git push origin --delete 001-smart-leaderboard